server.port=9090

# MCP server over STDIO (required for Claude Desktop / Cursor)
spring.ai.mcp.server.stdio=true

# No banner so stdout is not polluted (STDIO must stay clean for JSON-RPC)
spring.main.banner-mode=off

# Minimal console logging; use stderr only for STDIO servers
logging.pattern.console=
